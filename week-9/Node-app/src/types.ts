// Types is also used to aggregrate the data together.
// types is also similar to the interfaces
// we cannot implement a class by using types.

type Profile ={
     name:string,
     age:number,
     email?:string //optional
}
